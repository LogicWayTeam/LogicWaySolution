apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: preprod-ingress
  namespace: preprod
spec:
  ingressClassName: tailscale
  rules:
  - host: preprod
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
      - path: /logicway
        pathType: Prefix
        backend:
          service:
            name: logicway
            port:
              number: 8000
      - path: /route-engine
        pathType: Prefix
        backend:
          service:
            name: route-engine
            port:
              number: 8001
