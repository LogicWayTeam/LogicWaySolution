apiVersion: v1
kind: Service
metadata:
  name: logicway
  labels:
    app: logicway
  namespace: test
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "logicway-test"
spec:
  selector:
    app: logicway
  ports:
    - name: http-django-8000
      port: 80
      targetPort: 8000
  type: ClusterIP
